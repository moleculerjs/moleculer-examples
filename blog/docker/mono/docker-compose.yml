version: "3"

services:

  main:
    build:
      context: "../../"
    image: moleculer-blog
    expose:
      - "3000"
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      LOGGER: true
      LOGLEVEL: info
